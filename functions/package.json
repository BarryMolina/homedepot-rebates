{
  "name": "homedepot_receipts",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:submitReceipt": "functions-framework --target=submit-receipt",
    "start:rebateTracker": "functions-framework --target=rebate-tracker",
    "start": "functions-framework --target=main",
    "deploy": "gcloud functions deploy homedepot-rebate --gen2 --region=us-central1 --runtime=nodejs16 --source=./ --entry-point=main --trigger-http --memory=512MB"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/functions-framework": "^3.1.2",
    "puppeteer": "^16.1.0"
  }
}
